namespace KnifeHero.KnifeHeroCode.Powers;

/* Marker for FLAGS — The Gay Blade's pride-flag / status-flag statuses (Stealth, and more
   to come). Cards count and consume Flags. To make a power a Flag, implement this. */
public interface IFlag
{
}
